name: first-workflow
on: workflow_dispatch
jobs:
 first-job:
   runs-on:ubuntu-latest
   steops:
     - name: printing greeting
       run: echo "Hello World"
     - name: pringting goodbye
       run: echo "Goodbye World"
